export enum BasicStatus {
  DISABLE = 1,
  ENABLE
}

export enum FileType {
  Image = 'image',
  Video = 'video',
  Url = 'url'
}

export enum FileSourceType {
  Admin = 'admin',
  Client = 'client'
}

export enum PermissionType {
  CATALOGUE = 1,
  MENU,
  BUTTON
}
